<!--
  Архивная копия. Приложена к issue #2810 (ветка задач #2797 → #2808 → #2810).
  Оригинал лежал в недоступном извне репозитории ideav/slitterplan
  (docs/integram-app-workflow.md) — здесь он сохранён для сверки и отслеживания
  происхождения правил. Содержимое ниже воспроизведено без изменений.
-->

> **Архивная копия первоисточника.** Это первая попытка собрать единый документ по
> разработке Integram-приложений. По замечанию из issue #2797 в нём много ошибок,
> поэтому он **не является руководством к действию** — выверенные правила собраны в
> [`WORKSPACE_DEVELOPMENT_GUIDE.md`](WORKSPACE_DEVELOPMENT_GUIDE.md). Документ сохранён
> здесь как первоисточник для сверки и отслеживания происхождения правил (issue #2810).
> Оригинал находился в репозитории `ideav/slitterplan` (`docs/integram-app-workflow.md`).

# Полный процесс создания Integram-приложения

## 0. Что такое Integram-приложение

Integram — low-code платформа (ideav.ru). Приложение = база данных (workspace) + таблицы + HTML/JS шаблоны для UI. Данные хранятся в таблицах, интерфейс — кастомные HTML-страницы с JS, которые загружаются в файловое хранилище базы.

**Ключевые понятия:**
- **База данных (workspace)** — изолированное пространство с таблицами, файлами, пользователями
- **Таблица (type)** — аналог таблицы SQL, имеет ID, колонки, связи
- **Колонка (requisite/req)** — поле таблицы, имеет ID и алиас (русское имя)
- **Ссылка (ref)** — связь между таблицами (foreign key)
- **Запись (object)** — строка таблицы, имеет ID
- **Запрос (report/query)** — команда Интеграму на выборку и обработку данных
- **Шаблон (template)** — HTML-страница, загруженная в `/download/{db}/`, рендерится Integram

---

## Этап 0: Подключение к базе данных

Прежде чем начать разработку, агент должен получить доступ к базе Integram. Когда агент готов приступить к работе, он задаёт вопрос:

> **"Создай базу данных в Integram и пользователя `claude` в ней. Сообщи адрес базы и пароль пользователя."**

Пользователь создаёт базу на [ideav.ru](https://ideav.ru), затем:
1. Заходит в базу → Настройки → Пользователи
2. Создаёт пользователя с логином `claude` и произвольным паролем
3. Сообщает агенту: адрес базы (например `ideav.ru/myapp/`) и пароль

Агент выполняет авторизацию:
```bash
curl -X POST 'https://ideav.ru/{db}/auth?JSON=1' \
  -F 'login=claude' -F 'pwd={пароль}'
# Сохранить токен из ответа для последующих запросов
```

После получения токена агент самостоятельно работает с API базы.

---

## Этап 1: Уточнение требований

### 1.1 Вопросы, которые нужно задать

Перед началом проектирования задай эти вопросы (по одному):

1. **"Опиши бизнес-процесс: кто что делает и в каком порядке?"**
   - Понять роли (менеджер, оператор, руководитель)
   - Понять этапы процесса (заказ → планирование → исполнение → контроль)

2. **"Какие экраны/панели нужны? Кто на каждом работает?"**
   - Перечислить все интерфейсы
   - Привязать каждый к роли

3. **"Какие данные вводятся на каждом экране? Какие справочники нужны?"**
   - Выявить все сущности (таблицы)
   - Выявить все поля

4. **"Как связаны сущности между собой?"**
   - Определить ссылки (ref) между таблицами
   - Определить родительские/дочерние таблицы

### 1.2 Результат этапа

На выходе должны быть:
- Список ролей
- Список экранов с привязкой к ролям
- Список таблиц с полями и связями (черновик схемы)

---

## Этап 2: Проектирование схемы данных

### 2.1 Правила проектирования таблиц (из StructureRules.md)

1. **Первая колонка любой таблицы — собственный идентификатор объекта** (тип SHORT=3)
   - Это не ссылка! Это то, ЧТО такое эта запись
   - Примеры: название сырья, номер заказа, имя слиттера

2. **Имена таблиц — единственное число, на русском**
   - Правильно: "Сырьё", "Заказ", "Слиттер"
   - Неправильно: "Сырьё и материалы", "Orders"

3. **Колонки-ссылки (ref) создаются в таблице-источнике**
   - Если "Тип резки" ссылается на "Сырьё" → ref создаётся в "Тип резки"
   - Не создавай колонку в справочнике для обратной связи (это делает сама платформа)

4. **НЕ создавай колонку "Название"/"Имя"/"ФИО"** — это дублирует _value (первую колонку)

### 2.2 Типы колонок (t=...)

| Код | Тип | Описание |
|-----|-----|----------|
| 3 | SHORT | Короткий текст (названия, имена) |
| 8 | CHARS | Длинный текст |
| 9 | DATE | Дата (YYYY-MM-DD) |
| 4 | DATETIME | Дата и время |
| 11 | BOOLEAN | Да/нет |
| 12 | MEMO | Большой текст (описания, комментарии) |
| 13 | NUMBER | Число (цены, количества, метры) |
| 14 | SIGNED | Число со знаком |

### 2.3 Документирование схемы

Запиши схему в виде таблицы:

```markdown
### Таблица: Сырьё (RawMaterial)
| Поле | Тип | Описание |
|------|-----|----------|
| Название | SHORT (3) | Первая колонка — тип/название сырья |
| Ширина джамбо, мм | NUMBER (13) | Ширина исходного рулона |
| Остаток, м | NUMBER (13) | Остаток на складе |
| Описание | MEMO (12) | Дополнительная информация |

### Связи:
- Тип резки.Сырьё → Сырьё (ref)
```

### 2.4 Результат этапа

Файл со схемой всех таблиц, колонок и связей. Это станет основой для скрипта создания.

---

## Этап 3: Создание таблиц (скрипт)

### 3.1 Структура скрипта

Скрипт на bash, использующий curl для вызовов API Integram. Паттерн: [`create_perelidoz.ps1`](https://github.com/ideav/crm/blob/main/docs/create_perelidoz.ps1).

**Базовый шаблон функции `api_call`:**

```bash
api_call() {
    local endpoint="$1"
    local method="${2:-POST}"
    shift 2
    local url="${BASE_URL}/${DB}/${endpoint}?JSON=1"
    # ... добавляет token и _xsrf ко всем запросам
    curl -s -k -X POST "$url" -F "token=$TOKEN" -F "_xsrf=$XSRF" "$@"
}
```

**Критически важно:** функция `log()` должна писать в stderr (`>&2`), иначе вызовы `api_call` внутри `$()` будут захватывать логи вместе с JSON-ответом.

### 3.2 Последовательность API-вызовов

1. **Авторизация:**
   ```bash
   POST /{db}/auth?JSON=1
   Fields: login, pwd
   Returns: { token, _xsrf, id }
   ```

2. **Создание таблицы:**
   ```bash
   POST /{db}/_d_new?JSON=1
   Fields: t=3, val="Имя таблицы"
   Returns: { obj: tableId }
   ```

3. **Создание типа колонки:**
   ```bash
   POST /{db}/_d_new?JSON=1
   Fields: t=<typeCode>, val="Тип колонки"
   Returns: { obj: typeId }
   ```

4. **Добавление колонки в таблицу:**
   ```bash
   POST /{db}/_d_req/{tableId}?JSON=1
   Fields: t=<typeId>
   Returns: { id: reqId }
   ```

5. **Установка алиаса колонки:**
   ```bash
   POST /{db}/_d_alias/{reqId}?JSON=1
   Fields: val="Имя колонки"
   ```

6. **Создание ссылки (ref):**
   ```bash
   POST /{db}/_d_ref/{targetTableId}?JSON=1
   Returns: { obj: refTypeId }
   ```
   Затем добавить ref как колонку в таблицу-источник:
   ```bash
   POST /{db}/_d_req/{sourceTableId}?JSON=1
   Fields: t=<refTypeId>
   ```

### 3.3 Порядок создания таблиц

**Важно:** таблицы создаются в порядке зависимостей:
1. Сначала независимые справочники (Сырьё, Слиттер)
2. Потом зависимые справочники (Тип резки — ссылается на Сырьё)
3. Основные таблицы (Заказ)
4. Дочерние таблицы (Позиция заказа)
5. Таблицы событий (Производственная резка — ссылается на всё)

Ссылки (ref) добавляются после создания ВСЕХ таблиц.

### 3.4 Результат этапа

Файл `create_tables.sh`, который:
- Можно запустить повторно (идемпотентный — проверяет ошибки)
- Пишет лог в `create_tables.log`
- Выводит сводку всех созданных ID в конце

---

## Этап 4: Разработка шаблонов (HTML/JS)

### 4.1 Структура шаблона

Каждый шаблон = HTML-файл в `templates/` со встроенными стилями и подключением JS:

```html
<style>
/* Локальные стили */
.my-panel { ... }
</style>

<div class="my-panel">
    <!-- HTML интерфейса -->
</div>

<script>
window.SOME_TYPE_ID = 469;  // ID таблицы
</script>
<script src="/download/{_global_.z}/api.js?1"></script>
<script src="/download/{_global_.z}/tables.js?1"></script>
<script src="/download/{_global_.z}/my-panel.js?1"></script>
```

**Важно:** `{_global_.z}` — шаблонная переменная Integram, заменяется на имя базы данных при рендеринге.

### 4.2 JS-файлы

**api.js** — обёртка над API Integram:
```javascript
// createRecord(tableId, fields, parentId)
Api.createRecord(469, { 'Тип сырья': 'Бумага', 'Ширина джамбо, мм': 1020 });

// queryTable(typeId, where, sort, limit)
Api.queryTable(469, { 'Статус': 'Новый' }, '-id', 20);

// updateRecord(objId, fields)
Api.updateRecord(571, { 'Остаток, м': 4500 });
```

**tables.js** — общие функции для работы с таблицами (переиспользуются всеми панелями).

**Панельные JS** — логика конкретного экрана (order-entry.js, cutter-panel.js и т.д.).

### 4.3 Загрузка шаблонов на сервер

Шаблоны загружаются в папку `download` базы данных через веб-интерфейс или API:

```bash
POST /{db}/dir_admin
Fields:
  download=1
  add_path=
  upload=Загрузить
  userfile=@file.js
```

После загрузки файлы доступны по URL: `https://ideav.ru/download/{db}/file.js`

### 4.4 Результат этапа

- Папка `js/` с `api.js`, `tables.js` и панельными JS-файлами
- Папка `templates/` с HTML-шаблонами для каждого экрана
- Все файлы загружены и проверены через `curl https://ideav.ru/download/atex/имя_файла`

---

## Этап 5: Роли, пользователи и меню

### 5.1 Системные таблицы

Integram автоматически создаёт три системные таблицы:
- **Роль** (таблица 42, type=3) — название роли, описание, пункты меню
- **Пользователь** (таблица 18, type=3) — логин, роль (ref на Роль), пароль, email, телефон
- **Меню** (таблица 151, type=3) — пункты меню ролей (адрес, иконка, параметры)

**ВАЖНО:** Для системных таблиц (type=3) ВСЕ колонки передаются через префикс `t` + ID колонки.

### 5.2 Паттерн `t{column_id}=value`

Все значения передаются как `t{id_колонки}=value` — это единственный формат для всех таблиц (системных и обычных):

```bash
# Роль (таблица 42): t42=название, t135=описание
POST /{db}/_m_new/42?JSON=1
Fields: up=1, t42=менеджер, t135=Управление заказами и планирование

# Пользователь (таблица 18): t18=логин, t115=role_id, t20=пароль
POST /{db}/_m_new/18?JSON=1
Fields: up=1, t18=manager, t115=632, t20=slitter123

# Меню (таблица 151): t151=название, t153=адрес, t391=иконка
POST /{db}/_m_new/151?JSON=1
Fields: up=<role_id>, t151=Приём заказов, t153=order-entry
Icon: --form-string 't391=<i class="pi pi-file-edit"></i>'
# ВАЖНО: t391 содержит HTML-тег <i> с угловыми скобками и кавычками —
# использовать --form-string, иначе curl -F ломает значение.
```

**ID колонок для справки (получить через `GET /{db}/metadata?JSON=1`):**

| Таблица | Колонка | ID | Тип | Назначение |
|---------|---------|-----|-----|------------|
| Роль (42) | _value | t42 | - | Название роли |
| Роль (42) | Описание | t135 | 12 | Описание роли |
| Роль (42) | Объекты | t117 | 5 | Права на объекты |
| Пользователь (18) | _value | t18 | - | Логин |
| Пользователь (18) | Роль | t115 | ref→42 | Ссылка на роль |
| Пользователь (18) | Password | t20 | 6 | Пароль (хешируется сервером) |
| Пользователь (18) | Email | t41 | 3 | Email |
| Меню (151) | _value | t151 | - | Название пункта |
| Меню (151) | Адрес | t153 | 8 | URL/href страницы |
| Меню (151) | Иконка | t391 | 8 | HTML-тег `<i class="pi pi-..."></i>` |
| Меню (151) | Параметры | t158 | 3 | Доп. параметры |

### 5.3 Создание ролей

Типичные роли для производства:
- **менеджер** — создаёт заказы, планирует резку, видит всё
- **оператор** — работает с пультами размотки/резки, видит свои задания
- **руководитель** — смотрит диспетчер производства, отчёты

```bash
POST /{db}/_m_new/42?JSON=1
Fields: up=1, t42=менеджер
```

### 5.4 Создание пунктов меню

Пункты меню создаются как дочерние записи роли (`up=<role_id>`):

```bash
POST /{db}/_m_new/151?JSON=1
Fields: up=<role_id>, t151=Название, t153=page-name, t391=pi pi-icon
```

Иконки — CSS-классы PrimeIcons (https://primefaces.org/primeicons/):
- `pi pi-file-edit` — редактирование
- `pi pi-calendar` — календарь/планирование
- `pi pi-chart-bar` — графики/отчёты
- `pi pi-sliders-h` — настройки
- `pi pi-arrow-circle-down` — загрузка
- `pi pi-circle` — метка

### 5.5 Создание пользователей

```bash
POST /{db}/_m_new/18?JSON=1
Fields: up=1, t18=login, t115=<role_id>, t20=password
```

Пароль (`t20`) хешируется сервером автоматически. Проверить вход можно через:
```bash
curl -X POST 'https://ideav.ru/{db}/auth?JSON=1' -F 'login=manager' -F 'pwd=slitter123'
```

### 5.6 Удаление записей (если нужно)

```bash
POST /{db}/_m_del/{objId}?JSON=1
# Работает для любых таблиц, включая системные.
# При удалении пункта меню — автоматически удаляются все подчинённые пункты.
```

### 5.7 Источник

Паттерн `t{id}=value` взят из:
- https://github.com/ideav/crm/issues/632 — создание/удаление пунктов меню
- https://github.com/ideav/crm/issues/597 — неверное именование первой колонки при создании

### 5.8 Результат этапа

- 3 роли: менеджер, оператор, руководитель
- 3 пользователя с паролями (manager, operator, director / slitter123)
- Пункты меню для каждой роли с иконками и href-адресами
- Администратору (роль 145) добавлены все рабочие страницы

### 5.9 Права ролей на таблицы и запросы

Роль определяет уровни доступа ко всем объектам базы. После создания ролей и пользователей нужно задать права — какие таблицы и запросы доступны каждой роли.

Через реквизит **Объекты** (t117) роли указываются объекты (таблицы, запросы), к которым роль имеет доступ:

```bash
# Дать роли доступ к таблице (tableId) с уровнем WRITE
POST /{db}/_m_new/117?JSON=1
Fields: up=<role_id>, t117=<tableId или queryId>
```

Уровень доступа задаётся через реквизит у самого объекта доступа. Список объектов получить через `GET /{db}/metadata?JSON=1`.

**Системный пользователь admin** (имя совпадает с именем базы) имеет полные права без ограничений.

> **Внимание:** системные объекты Пользователь, Роль, Запрос, Меню — удалять категорически не рекомендуется.

---

## Этап 5b: Запросы (report/)

Запросы — правила выборки и обработки данных, настраиваются в Конструкторе запросов.

### Вызов запроса из шаблона

```bash
GET /{db}/report/{имяЗапроса}?JSON_KV    # [{\"Поле\":\"значение\",...}]
GET /{db}/report/{имяЗапроса}?JSON_DATA  # стандартный JSON Integram
GET /{db}/report/{id}?JSON_KV            # по числовому ID запроса
```

### Фильтрация FR_ / TO_

```bash
# Фильтр «от» (значение от / точное совпадение)
report/{name}?FR_MANAGER=Света
report/{name}?FR_СтатусID=9002

# Фильтр «до» (для диапазонов дат/чисел)
report/{name}?TO_DATE=31.12.2026

# Несколько значений
report/{name}?FR_СтатусID=IN(9002,44895)

# Комбинация
report/{name}?FR_MANAGER=Света&FR_DATE=09/09/2016
```

**Безопасность:** `FR_`/`TO_` работает только для колонок, у которых задано «Имя в отчете» — это защита от принудительной замены фильтра со стороны пользователя.

### Использование в JS-шаблонах

```javascript
// Загрузить данные запроса
const rows = await fetch(`/${db}/report/МойЗапрос?JSON_KV`).then(r => r.json());

// С динамическим фильтром
const filtered = await fetch(`/${db}/report/МойЗапрос?JSON_KV&FR_СтатусID=${statusId}`)
  .then(r => r.json());

// Вызов запроса как действие (триггер)
await fetch(`/${db}/report/ОтозватьПриглашение?FR_InviteID=${inviteId}`);
```

### Запросы на изменение данных

К отчёту можно добавить операцию SET — массово изменить данные у всех отфильтрованных записей. Применяется в два этапа: сначала превью результата, затем подтверждение:

```bash
POST /{db}/report/{name}?confirmed   # выполнить изменение
```

---

## Этап 6: Наполнение тестовыми данными

### 6.1 Скрипт наполнения

Создаётся отдельный bash-скрипт (паттерн: `seed_data.sh`), который:
1. Авторизуется
2. Создаёт записи через `_m_new/{tableId}` с указанием значений полей по алиасам

### 6.2 Принципы создания тестовых данных

1. **Сырьё** — 3-5 видов с разной шириной джамбо и остатками
2. **Слиттеры** — 3-4 слиттера с разными статусами (Свободен, Обслуживание)
3. **Типы резки** — по 1-2 на каждый вид сырья, разные втулки и намотки
4. **Заказы** — 3-5 заказов от разных заказчиков, разные даты и статусы
5. **Позиции заказа** — 2+ позиции на заказ, разное количество рулонов
6. **Производственная резка** — для каждого заказа свои резки на слиттерах, разные статусы (Ожидает, Наладка, В работе, Завершён)

### 6.3 API для создания записей

```bash
# Корневая запись (up=1)
POST /{db}/_m_new/{tableId}?JSON=1
Fields: up=1, t{tableId}="Название", t{colId1}=значение, t{colId2}=значение

# Дочерняя запись (up=parentId)
POST /{db}/_m_new/{tableId}?JSON=1
Fields: up=parentId, t{tableId}="Название", t{colId1}=значение

# Установка ref-поля (значение = ID записи из целевой таблицы)
Fields: t{colId}=579  # где 579 — ID записи в целевой таблице
```

### 6.4 Результат этапа

Файл `seed_data.sh`, который создаёт полный набор тестовых данных для демонстрации всех экранов.

---

## Этап 7: Проверка и запуск

### 7.1 Чек-лист проверки

- [ ] Все таблицы созданы (проверить через `edit_types` или меню "Таблицы")
- [ ] Все шаблоны загружены и открываются через `/download/atex/`
- [ ] Тестовые данные созданы во всех таблицах
- [ ] Связи между таблицами работают
- [ ] Роли и пользователи созданы (если применимо)
- [ ] Каждый экран открывается в браузере и показывает данные

### 7.2 Порядок проверки экранов

1. **Ввод заказов** — создать тестовый заказ через UI
2. **Планирование резки** — оптимизировать и распределить заказы по слиттерам
3. **Диспетчер производства** — посмотреть все резки по статусам
4. **Пульт размотки** — отметить выполнение размотки
5. **Пульт резки** — отметить выполнение резки
6. **Пульт втулкореза** — посмотреть задания на втулки

---

## Быстрый старт (шпаргалка для следующего проекта)

```
1. Уточнить требования (роли → экраны → сущности → поля → связи)
2. Спроектировать схему (таблица с полями и типами)
3. Написать create_tables.sh по шаблону из Этапа 3
4. Запустить create_tables.sh
5. Обновить Type ID в шаблонах HTML
6. Загрузить все файлы в /download/{db}/
7. Написать seed_data.sh
8. Запустить seed_data.sh
9. Создать роли и пользователей
10. Проверить все экраны
```

### Ключевое правило: единый формат для всех таблиц

Разницы между системными и обычными таблицами нет. Колонки передаются одинаково:

| Поле | Формат | Пример |
|------|--------|--------|
| _value (название) | `t{tableId}=Значение` | `t42=менеджер` |
| Любая колонка | `t{colId}=Значение` | `t135=Описание роли` |
| Ref-поле | `t{colId}=ID` | `t115=632` |

**Системные таблицы:** Роль (42), Пользователь (18), Меню (151), и все с type=3 в metadata.

### Полезные ссылки

- [StructureRules.md](https://github.com/ideav/crm/blob/main/docs/StructureRules.md) — правила структуры таблиц
- [MCP.md](https://github.com/ideav/crm/blob/main/docs/MCP.md) — описание API (неполное)
- [Roadmap.md](https://github.com/ideav/crm/blob/main/docs/Roadmap.md) — roadmap с примерами API-вызовов
- [create_perelidoz.ps1](https://github.com/ideav/crm/blob/main/docs/create_perelidoz.ps1) — эталонный скрипт создания таблиц и данных
- [Issue #632](https://github.com/ideav/crm/issues/632) — создание/удаление пунктов меню (_m_new/151, t153, t391)
- [Issue #597](https://github.com/ideav/crm/issues/597) — паттерн t{id} для первой колонки системных таблиц
